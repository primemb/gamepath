[package]
name = "gamepath-service"
version = "0.1.0"
edition = "2024"
description = "Privileged Windows network service for GamePath"

[dependencies]
gamepath-engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target.'cfg(windows)'.dependencies]
windows-service = "0.8.1"

